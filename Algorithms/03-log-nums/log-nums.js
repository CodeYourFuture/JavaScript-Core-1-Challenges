// Write code to print all numbers from 1 to `num`
// Assume `num` will be a positive number

let logNums = function (num) {
  let highestNumber = num;
  for (let i = 1; i < highestNumber + 1; i++) {
    console.log(i);
  }
};
